# Everything in Python is an object
#
# Each object has:
# 1. Identity (ID): a unique value that identifies the object in memory.
# 2. Type: defines the kind of data (e.g., int, str, float).
# 3. Value: the actual data stored in the object (can also be empty)
